# Dyad setup

Open the repo root in Dyad.

Run:
- `yarn dev` (web)
- `yarn worker:dev` (worker)

Update `apps/collab-worker/wrangler.toml` with your LiveKit URL/key/secret.

This scaffold is designed so Dyad can run the web app and you can run the worker in a terminal if needed.
